"use client"

import { LiveQueuePage } from "@/components/live-queue/LiveQueuePage"

export default function LiveQueueFormPage({ params }: { params: { formId: string } }) {
  return <LiveQueuePage />
}
